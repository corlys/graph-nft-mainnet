type Token @entity {
  id: ID!
  tokenId: BigInt!
  tokenURI: String!
  # externalURL: String!
  # ipfsURI: String!
  owner: Account!
}

type Account @entity {
  id: ID!
  tokens: [Token!]! @derivedFrom(field: "owner")
}
